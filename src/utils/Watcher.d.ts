import moveFiles from './move-files';
export default class Watcer {
    private moveFiles;
    private watcher;
    private pathToMonitor;
    constructor(moveFiles: moveFiles);
    openWatcher(): void;
}
